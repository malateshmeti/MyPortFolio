{
  "version": 2,
  "builds": [
    {
      "src": "my-portfolio/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "my-portfolio/build/$1" }
  ]
}
